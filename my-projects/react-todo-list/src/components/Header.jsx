const TopBar = () => {
  const headerStyle = {
    color: "black",
    textAlign: "center",
    fontFamily: "Arial, Sans-serif",
    fontWeight: "100",
  };

  return <h1 style={headerStyle}>TODO LIST</h1>;
};

export default TopBar;
